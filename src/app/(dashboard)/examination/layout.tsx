import { CoeShell } from "@/modules/examination/components/CoeShell";

export default function ExaminationLayout({ children }: { children: React.ReactNode }) {
  return <CoeShell>{children}</CoeShell>;
}
